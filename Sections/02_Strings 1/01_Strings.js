/**
 * Strings
 * 
 * → You can create a string in JavaScript by using the double quotes (") or single
 * quotes (').
 * 
 * → Strings are zero-indexed, meaning the first character is at position 0, the second 
 * at position 1, and so on.
 * 
 */

'use strict';

console.log("This is a string!");
console.log('This is another string!');