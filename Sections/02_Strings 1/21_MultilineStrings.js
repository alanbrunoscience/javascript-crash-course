/**
 * Multiline strings
 * 
 * - Unlike single-quote and double-quote strings, template strings can span multiple lines. 
 * 
 */

let text = `This is a multiline
string that
just works!`; // This would have been impossible with a normal string (single quotes or double quotes).

console.log(text);