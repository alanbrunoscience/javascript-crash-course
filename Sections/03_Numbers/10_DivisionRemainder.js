/**
 * Division Remainder
 * 
 * → You can also use the remainder operator '%' which returns the division remainder. Here's
 * an example:
 * 
 */

'use strict';

console.log(8 % 2); // Division remainder is 0 (8 = 4 * 2 + 0)
console.log(7 % 2); // Division remainder is 1 (7 = 3 * 2 + 1)