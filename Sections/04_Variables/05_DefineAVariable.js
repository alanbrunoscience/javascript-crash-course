/**
 * Define a variable 
 * 
 * → Define a variable called 'count' with an original value of '0' and then increment 
 * it (add '1' to it) on the following line.
 * 
 */

'use strict';

// define a variable "count" with value 0
let count = 0;
console.log(count); // 0

// then increment it
count++; // count += 1 or count = count + 1
console.log(count); // 1